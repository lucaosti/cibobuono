# Lightweight dependencies for CI (tests + data validation).
# Does NOT include ML libs (torch, faster-whisper, llama-cpp-python, gliner)
# which require GPU drivers or compilation and are only needed locally.

# Data validation
pydantic>=2.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Used by pipeline scripts imported during tests
psutil>=5.9.0
geopy>=2.4.0
thefuzz[speedup]>=0.22.0
rich>=13.0.0
yt-dlp>=2024.0.0
